#include <stdio.h>

int main()
{
    int a,b,c; 
    
    printf("Input two numbers to add them\n");
    scanf("%d%d", &a, &b);
    c = a + b;
    
    printf("The sum of these two numbers is = %d\n", c);
    return 0;
}

